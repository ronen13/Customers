services:
  - type: web
    name: grants-portal
    env: node
    buildCommand: npm install && npm run build
    startCommand: npm run start
    buildFilter:
      paths:
        - src/**
    envVars:
      - key: NODE_VERSION
        value: 18
